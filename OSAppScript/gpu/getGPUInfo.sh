#!/bin/bash
echo "####################################################################"
lspci -v | grep -i vga | grep -i -E "Nvidia|Intel" | awk '{print $1}' | xargs -I{} lspci -v -s {}
echo "####################################################################"
lshw -c video
echo "####################################################################"
deviceQuery
echo "####################################################################"
isNvidia=`nvidia-smi -q | grep "failed"`
if [ -z "$isNvidia" ];then
	nvidia-smi
else
	echo "No Nvidia"
fi
echo "####################################################################"
